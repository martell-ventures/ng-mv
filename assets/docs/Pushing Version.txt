To push a new version, you would use:

git tag -a v0.0.20 -m "Added facebook-post-button."
git push origin v0.0.20

Be sure to change teh version in bower.json!

To list the current tags:

git tag -l
