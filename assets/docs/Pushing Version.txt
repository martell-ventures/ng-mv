To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.43 -m "Upgraded angular and made country loader use new request format"
git push origin v0.0.43

Then:
git push

To list the current tags:

git tag -l
