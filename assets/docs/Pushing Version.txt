To push a new version, you would use:

git tag -a v0.0.19 -m "19 - Added optional callback for address field changes (vs. watching). (18 was problematic)"
git push origin v0.0.19

Be sure to change teh version in bower.json!

To list the current tags:

git tag -l
