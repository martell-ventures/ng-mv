To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.40 -m "Modified Credit card stuff to autofill with Safari."
git push origin v0.0.40

Then:
git push

To list the current tags:

git tag -l
