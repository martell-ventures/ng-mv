To push a new version, you would use:

git tag -a v0.0.21 -m "Ability to request additional permissions on Facebook login."
git push origin v0.0.21

Then:
git push

Be sure to change the version in bower.json!

To list the current tags:

git tag -l

This line is here only for testing.  Remove later.