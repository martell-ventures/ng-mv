To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.24 -m "Fix file uploading when default preview image not specified"
git push origin v0.0.24

Then:
git push

To list the current tags:

git tag -l
