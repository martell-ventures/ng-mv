To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.41 -m "Facebook login and profile picture improvements"
git push origin v0.0.41

Then:
git push

To list the current tags:

git tag -l
