To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.33 -m "Fix issue with mv-address where a slow loading countries.json might cause problems."
git push origin v0.0.33

Then:
git push

To list the current tags:

git tag -l
