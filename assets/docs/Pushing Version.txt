To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.22 -m "Added missing Canadian Provinces and Territories to country.json"
git push origin v0.0.22

Then:
git push

To list the current tags:

git tag -l

This line is here only for testing.  Remove later.