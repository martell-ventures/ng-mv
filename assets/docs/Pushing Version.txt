To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.48 -m "Internal: Fix mismatch on version in bower and in here."
git push origin v0.0.48

Then:
git push

To list the current tags:

git tag -l
