To push a new version, you would use:

Change the version in bower.json to the new version

git tag -a v0.0.46 -m "Facebook: Added version and new URL per their documentation"
git push origin v0.0.46

Then:
git push

To list the current tags:

git tag -l
